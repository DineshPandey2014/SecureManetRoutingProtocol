var PORTFOUR = 55555;
var HOSTFOUR = '127.0.0.1';

var dgram = require('dgram');
var server = dgram.createSocket('udp4');

console.log("I am here for UDP");


server.on('listening', function () {
    var address = server.address();
    console.log('UDP Server listening on ' + address.address + ":" + address.port);
});

var PORTNODETHREE = 44444;
var HOSTONE = '127.0.0.1';
var client = dgram.createSocket('udp4');
server.on('message', function (message, remote) {
    console.log(remote.address + ':' + remote.port +' - ' + message);
    console.log("Node Four-->"+ message);
    message = "Hacked The Data";
    var nodeOne = new Buffer(message);
    console.log("Node Four After Buffer-->"+ nodeOne);
    client.send(nodeOne, 0, nodeOne.length, PORTNODETHREE, HOSTONE, function(err, bytes) {
        if (err) throw err;
        console.log('UDP message sent to ' + HOSTONE +':'+ PORTNODETHREE);
        //client.close();
    });
    //socket.setBroadcast

});

server.bind(PORTFOUR, HOSTFOUR)

